int compteL (char *str)
{
    int (i=0);
    for ( ; str [i] = NULL; i++){
        ;
        }
    return (i);
}


void my_print_revert (char* toRevert)
{
    x=compteL (toRevert)
    for (; toRevert[x])]>=0; x-- ){
        print_char(toRevert[x]);
    }
}   




int main (void)
{
   my_print_n_ascii(90) ;
}